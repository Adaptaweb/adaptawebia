---
interface Props {
  class?: string;
  size?: number;
}

const { class: className, size = 40 } = Astro.props;
---

<img src="/logo.webp" width={size} height={size} alt="AdaptaWeb" class={className} />
